# Cheatsheet

- Go's standard library provides the strconv package for converting between strings and basic values
- strconv.Atoi means ASCII to integer.
- The important thing is that Atoi returns two values: int, error